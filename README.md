# TCP Hex Sender

Minimal desktop GUI app in Python for sending raw HTTP request bytes over TCP and viewing request/response in hex + bytes preview.

## Requirements

- Python 3.10+
- PySide6

## Run

1. Install dependencies:

   ```bash
   pip install -r requirements.txt
   ```

2. Start the app:

   ```bash
   python app.py
   ```
